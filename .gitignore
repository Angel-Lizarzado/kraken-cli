# Dependencias de Node
node_modules/

# Archivos de entorno y secretos
.env
.env.local

# Builds de Electron (donde se generan los .exe)
dist/
build/
out/
release/

# Staging de ofuscación (generado por npm run obfuscate — NO committear)
.build-obfuscated/

# Archivos de logs temporales (los que creamos para MySQL, etc.)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# CARPETA DE BACKUPS (¡CRÍTICO!)
# Ajusta el nombre si tu carpeta local se llama distinto
respaldos/
backups/
/tmp/

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Carpetas de build y compilación
dist/
dist_old/
release/
build/
.build-obfuscated/
*.exe
*.asar

# OpenCode / OpenClaw configs y skills
.opencode/
.openclaw/
*.log